version: '3.8'

services:
  localstack:
    image: localstack/localstack:3.0
    ports:
      - "4566:4566"
    environment:
      SERVICES: sqs,sns
      DEBUG: 1
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
